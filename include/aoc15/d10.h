#pragma once

#include <string>
#include <string_view>

namespace Aoc15
{
auto solveD10P1(std::string_view in) -> size_t;

auto solveD10P2(std::string_view in) -> size_t;

auto lookAndSay(std::string_view s) -> std::string;
}